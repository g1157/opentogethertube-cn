新版入口：http://150.158.143.199:18080

旧版入口：http://150.158.143.199:8080

新版基于官方 v0.15.0 源码，默认简体中文。永久房间在最后一人离开时暂停，
保存当前视频、播放进度和待播列表，再次进入时自动恢复。公开的永久房间休眠后
仍出现在房间列表中；不公开列出的房间继续通过原房间路径访问。

全屏按钮只将播放器与播放控件切换到全屏，不再把整张房间网页放大。全屏时
锁定背景滚动，退出时恢复页面位置；浏览器拒绝原生全屏时使用网页内全屏。
影院模式仍然允许浏览下方列表，两者用途不同。

服务器上的新版目录是 `/home/ubuntu/ott-next`，使用独立的 Compose 项目、
PostgreSQL、Redis、数据卷和登录 Cookie。原账号数据已复制到新版；以后两个
实例的数据分别保存。旧版的目录、服务和数据库不参与新版更新。

项目没有默认的全站管理员登录。普通账号登录后可认领尚无房主的房间，房主
可以管理该房间并分配房间管理员。运维 API 密钥保存在新版目录的 `.env` 中，
这不是网页登录密码。

切回旧版只需使用 8080 入口。需要停用新版时，在新版目录执行：

```sh
sudo docker compose -f compose.yml stop
```

此操作保留新版数据卷，也不会停止旧版。两个实例后续产生的数据不会自动合并。

新版数据库每天自动备份到 `backups/`；手动备份可执行 `./backup.sh`。
初次部署前的原数据库和永久房间缓存快照也保存在此目录中。

新版沿用 HTTP 端口访问。正式长期使用时建议配置域名和 HTTPS。链接解析兼容性
按本次要求暂未修改。
